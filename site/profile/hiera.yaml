---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Server Application Code"
    path: "serverappcode/%{facts.serverappcode}.yaml"
 
  - name: "Common data"
    path: "common.yaml"
